import React, {PropTypes} from 'react'

// const Header = ({Header}) => {
const StreamList = props => {
  return (
    <div>
      <h1>My Array or List</h1>
    </div>
  )
}

// Header.propTypes = {
//   Header: PropTypes.object.isRequired
// }

export default StreamList;
